// All API calls are relative ("/api/..."); ng serve proxies them to the .NET API
// (see proxy.conf.json). In production the same origin serves both, so this still works.
export const API_BASE = '/api';
